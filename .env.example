# Copy this file to .env and fill in your values
# Expo + react-native-dotenv expects KEY=VALUE format

# API endpoint for the dashboard to fetch data from
# Example for local dev
API_URL=http://localhost:3000/api/data

# Example for production
# API_URL=https://your-domain.example.com/api/data
